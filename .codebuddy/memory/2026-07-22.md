## 2026-07-22

### Removed Docker dependency
- Deleted `docker-compose.yml` (MariaDB + Redis + Adminer containers)
- Deleted `.devcontainer/` directory (VS Code dev container config)
- Updated `backend/.env.example`: changed `DATABASE_URL` port from `3307` (Docker mapping) to `3306` (native MariaDB default)
- Updated `README.md`: removed Docker from prerequisites, rewrote setup section to describe native MariaDB + Redis installation, removed `docker-compose.yml` from directory tree
- No code changes needed in `backend/src/` — all database/redis connections are driven by env vars

### Fixed markdownlint warnings (11 → 0)
- MD040: Added language specifiers to fenced code blocks
- MD060: Fixed table column pipe alignment
- MD031: Added blank line before fenced code block
- MD034: Wrapped bare URLs in angle brackets/backticks

### Migrated to Next.js for Vercel deployment
- Converted Express backend to Next.js App Router API routes (41 routes)
- Replaced ioredis with @upstash/redis (Vercel-compatible Redis)
- Replaced local file uploads (multer) with Vercel Blob storage
- Replaced jsonwebtoken with jose (Edge-compatible JWT)
- Migrated Vite+React frontend to Next.js App Router with route groups:
  - `(erp)` — ERP dashboard with role-based access (Admin/Manager/Staff)
  - `(shop)` — Public storefront, cart, orders, customer auth
- Copied shadcn UI components, i18n locales, auth contexts, cart context
- Build succeeds: 21 static pages + 20 dynamic API routes, 87.5kB shared JS
- Configuration: vercel.json, next.config.js, tailwind.config.js, postcss.config.js
- Old backend/ and frontend/ directories retained but excluded from build
